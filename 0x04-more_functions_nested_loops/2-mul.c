#include <stdio.h>
#include "main.h"
/**
 *mul - give the product of two numbers
 *
 *@a: the first number
 *@b: the second number
 * Return: the product of two numbers
*/
int mul(int a, int b)
{
int mult;
mult = a * b;
return (mult);
}
